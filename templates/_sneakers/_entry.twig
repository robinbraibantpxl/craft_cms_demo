{% extends '_layout.twig' %}

{% set image = entry.image.one() %}

{% block content%}

{% if image %}
    <img src="{{image.url}}" alt="">
{% endif %}


{% include '_snippets/_blockBuilder.twig' %}
{% endblock %}